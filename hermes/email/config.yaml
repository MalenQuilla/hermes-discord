profile_name: email
role: kanban-worker

model:
  provider: openrouter
  default: anthropic/claude-sonnet-4-6

skills:
  enabled:
    - google-workspace
    - kanban-worker
  config:
    google-workspace:
      gmail:
        client_id: ${GMAIL_CLIENT_ID}
        client_secret: ${GMAIL_CLIENT_SECRET}
        refresh_token: ${GMAIL_REFRESH_TOKEN}
        user: ${GMAIL_USER}
        draft_only: ${EMAIL_DRAFT_ONLY}

# Pre-tool hook enforces draft-only globally
hooks:
  pre_tool_call:
    - command: /opt/data/hooks/pre_tool.sh
      timeout: 10

allowed_toolsets:
  - mcp:google-workspace

storage:
  memories_dir: /opt/data/runtime/profiles/email/memories
  sessions_dir: /opt/data/runtime/profiles/email/sessions
